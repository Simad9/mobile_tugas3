import 'package:flutter/material.dart';

class TrackingLbsPage extends StatelessWidget {
  const TrackingLbsPage({super.key});

  @override
  Widget build(BuildContext context) {
     return Scaffold(
      appBar: AppBar(
        title: Text("Tracking LBS Page"),
      ),
      body: Text("Tracking LBS Page "),
    );
  }
}